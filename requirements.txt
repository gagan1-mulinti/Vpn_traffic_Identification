# Core dependencies for training and live detection
# Use compatible versions for broad platform support. Pin exact versions if you
# need reproducible environments.
pandas>=1.5.3,<=2.1.0
numpy>=1.24.0,<=2.4.2
joblib>=1.2.0
scikit-learn>=1.2.2
nfstream>=6.6.0

# Optional utilities
tqdm>=4.60.0    # progress bars (optional)
python-dateutil>=2.8.2
